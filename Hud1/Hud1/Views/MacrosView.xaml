<UserControl x:Class="Hud1.Views.MacrosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Hud1.Views"
             xmlns:local="clr-namespace:Hud1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid DataContext="{Binding Path=MacrosViewModel, RelativeSource={RelativeSource AncestorType=local:MacrosView}}">
        <StackPanel views:Spacing.Vertical="8">
            <Label Content="Macros" Style="{DynamicResource TitleContent}"/>

            <ItemsControl ItemsSource="{Binding Macros}"
                      Margin="0,0,0,0"
                      BorderBrush="Transparent" BorderThickness="0"
                      Background="Transparent">
                <ItemsControl.ItemTemplate>
                    <DataTemplate >
                        <StackPanel Margin="0,0,0,8">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,0">
                                                <GradientStop Color="{DynamicResource ColorBackgroundDarkMed}" Offset="0"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Path=Selected}" Value="True">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,0">
                                                        <GradientStop Color="{DynamicResource ColorMedHalf}" Offset="0"/>
                                                    </LinearGradientBrush>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <DockPanel Margin="4,4,4,4">
                                <views:GlowBorderLabel DockPanel.Dock="Right" Label="{Binding Path=RightLabel}" Selected="{Binding Path=Selected}"/>
                                <Label VerticalAlignment="Center" HorizontalAlignment="Left" Style="{DynamicResource InfoLabel}" Content="{Binding Path=Name}" />
                                <Label Margin="5,0,5,0" VerticalAlignment="Center" Style="{DynamicResource InfoLabel}" HorizontalAlignment="Left" Content="{Binding Path=Log}" />
                            </DockPanel>
                            <Grid MaxWidth="450" HorizontalAlignment="Left">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=Error}" Value="">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Label Margin="5,0,5,0" FontSize="10" VerticalAlignment="Center" Style="{DynamicResource InfoLabel}" HorizontalAlignment="Left" Content="{Binding Path=Error}" />
                            </Grid>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </Grid>
</UserControl>
